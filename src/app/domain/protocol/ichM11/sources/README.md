# ICH M11 static source documents

Runtime PDFs are served from `public/reference/` (see `ichM11ReferenceDocuments.ts`).

Optional: keep authoring copies of source PDFs in this folder for documentation. Parsed section data lives in `ichM11Template.ts` and `ichM11TemplateSectionReference.ts`.
